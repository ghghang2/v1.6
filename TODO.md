# Refactoring Progress

- [x] Load configuration from `repo_config.yaml` in `run.py`.
- [x] Replace hard‑coded constants (`REPO`, `MODEL`, `SERVER_URL`) with values from `config`.
- [ ] Verify that `run.py` still starts the llama‑server correctly.
- [ ] Run tests to ensure no regressions.
- [ ] Commit changes and push to GitHub.
- [ ] Send completion email.
